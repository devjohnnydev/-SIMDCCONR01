[phases.setup]
nixPkgs = ["python311", "git"]
aptPkgs = [
    "libpango-1.0-0",
    "libpangoft2-1.0-0",
    "libcairo2",
    "libharfbuzz0b",
    "libffi-dev",
    "libgdk-pixbuf-2.0-0",
    "shared-mime-info",
    "fontconfig"
]

[variables]
PYTHONUNBUFFERED = "1"
