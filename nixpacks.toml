[phases.setup]
nixPkgs = [
    "python311", 
    "git",
    "pango",
    "cairo",
    "glib",
    "gdk-pixbuf",
    "libffi",
    "fontconfig",
    "gobject-introspection",
    "pkg-config",
    "gcc"
]
aptPkgs = [
    "shared-mime-info"
]

[variables]
PYTHONUNBUFFERED = "1"
