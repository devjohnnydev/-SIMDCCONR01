[phases.setup]
nixPkgs = ["python311", "git", "glib", "pango", "cairo", "harfbuzz", "gdk-pixbuf", "fontconfig", "libffi", "shared-mime-info"]
nixLibs = ["glib", "pango", "cairo", "harfbuzz", "gdk-pixbuf", "libffi"]

[variables]
PYTHONUNBUFFERED = "1"
LD_LIBRARY_PATH = "$LD_LIBRARY_PATH:/usr/lib/x86_64-linux-gnu"
